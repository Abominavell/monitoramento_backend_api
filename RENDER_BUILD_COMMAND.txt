Copie UMA das duas linhas abaixo (sem aspas) para Build Command no Render.
Nao use reticencias (... ou …) no comando.

Opcao A (recomendado):
chmod +x build.sh && ./build.sh

Opcao B (sem build.sh):
pip install -r requirements.txt && python manage.py collectstatic --noinput && python manage.py migrate --noinput && python manage.py ensure_superuser
